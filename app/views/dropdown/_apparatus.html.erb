<%= form_for @add_apparatus, :url => { :action => "update_apparatus"}, :html => {:method => :put}  do |f| %>
    <% if @apparatus.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@apparatus.errors.count, "error") %> prohibited this change from being saved:</h2>

          <ul>
            <% @apparatus.errors.full_messages.each do |msg| %>
                <li><%= msg %></li>
            <% end %>
          </ul>
        </div>
    <% end %>
    <p>
    <div class="field">
      <%= f.label 'Add Apparatus' %>
      <%= f.text_field :add_apparatus %>
    </div>
    <%= f.submit %>

<% end %>