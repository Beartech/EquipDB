<%= form_for @add_category, :url => { :action => "update_categories"}, :html => {:method => :put}  do |f| %>
    <% if @categories.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@categories.errors.count, "error") %> prohibited this change from being saved:</h2>

          <ul>
            <% @categories.errors.full_messages.each do |msg| %>
                <li><%= msg %></li>
            <% end %>
          </ul>
        </div>
    <% end %>
<p>
    <div class="field">
      <%= f.label 'Add Category' %>
      <%= f.text_field :add_category %>
    </div>
      <%= f.submit %>

<% end %>