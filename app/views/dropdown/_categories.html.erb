<%= form_for :categories, :url => { :action => 'update_categories'}, :html => {:method => :put}  do |f| %>
    <% if @category.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@category.errors.count, 'error') %> prohibited this change from being saved:</h2>

          <ul>
            <% @category.errors.full_messages.each do |msg| %>
                <li><%= msg %></li>
            <% end %>
          </ul>
        </div>
    <% end %>
<p>
    <div class="form-inline">
      <%= f.radio_button 'delete_me', '0', :checked => true %>
      <%= label 'delete_me', 'Add Category' %>
      <%= f.radio_button 'delete_me', '1' %>
      <%= label 'delete_me', 'Delete Category' %>
    </div>
    <div class="field">
      <%= f.text_field :mod_category %>
    </div>
      <%= f.submit %>

<% end %>