

  <%= link_to '<- Delete (remove from inventory)', adjust_inventory_path(:inventory => {:part => @inventory_transaction.array, :operator => '-'}) , :method => :post, :remote => true, :class => 'itemDelete' %>

